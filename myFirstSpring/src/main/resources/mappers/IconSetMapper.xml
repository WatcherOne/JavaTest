<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.watcher.mapper.IconSetMapper">

    <select id="getIconListByClassify" resultType="com.watcher.entity.IconSet">
        select * from icon_set where classify_id = #{classifyId}
    </select>
</mapper>
